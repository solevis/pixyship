// Basically empty for now
// Holding code that may be useful someday

//  if (ninepatch) {
//    obj = {
//      borderImageSource: `url('//pixelstarships.s3.amazonaws.com/${s.source}.png')`, // -${s.x}px -${s.y}px`,
//      borderImageWidth: 10,
//      borderImageSlice: 10,
//      borderImageRepeat: 'repeat',
//      width: `${s.width}px`,
//      height: `${s.height}px`,
//      imageRendering: 'pixelated'
//    }
//  }
