services:
  backend:
    build: .
    ports:
      - "${BACKEND_PORT:-5000}:5000"
    environment:
      - FLASK_DATABASE_URI=postgresql://pixyship:pixyship@db:5432/pixyship
    command: python run.py
    stop_signal: SIGINT
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: postgres:15
    ports:
      - "${DB_PORT:-5432}:5432"
    environment:
      - POSTGRES_USER=pixyship
      - POSTGRES_PASSWORD=pixyship
      - POSTGRES_DB=pixyship
      - PGDATA=/var/lib/postgresql/data/
    volumes:
      - ./pgdata:/var/lib/postgresql/data
